namespace WaveCell.RuleEngine.Strategy
{
    // Filter DTO.
    // You may use object[] as a type parameter if you hate type safety
    public record struct StrategyFilter(string? Filter1, string? Filter2, string? Filter3, string? Filter4);
}